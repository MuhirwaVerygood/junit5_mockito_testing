spring:
  application:
    name: junit_mockito_testing

  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: verygood
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true

    database: postgresql

server:
  port: 5000